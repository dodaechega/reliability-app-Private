# 슬롭·성능·에이전트 DX·검증 개선 (2026-09-05)

기준 커밋: `fcbbbdb`. 기존 Streamlit/SQLite 구조를 유지했다.

| 발견 사항 | 반영 내용 | 검증 |
|---|---|---|
| 데이터 저장 후 이력 기록 실패 시 추적 불일치 | 동일 트랜잭션에서 변경·이력 기록, 예외 시 롤백/연결 종료 | 생성·항목·템플릿·메타·삭제별 이력 실패 주입 |
| 빈 템플릿이 재시작 때 재시드됨 | 신규 DB만 시드, `user_version=1` 마이그레이션 | 빈 기존 DB·신규 DB·stage 백필 |
| 초기화 실패 시 부분 스키마가 남음 | 초기화 잠금과 DDL/시드 단일 트랜잭션 | 시드 실패 후 빈 스키마 확인·재시도 |
| 매 rerun마다 초기화, 통계 N+1 조회, 중복 항목 조회 | 초기화 캐시, 통계 일괄 조회, 조회 인덱스, 이미 읽은 항목 재사용 | 쿼리 수/인덱스 사용·통계 값 |
| 매 편집마다 엑셀 생성 | 저장 내용 기준 최대 32개 캐시, 다운로드 rerun 제외 | 반복 실행 1회 생성·저장 후 재생성 |
| 두 편집 화면의 중복 정제와 빈값 처리 | 공통 정제 함수, pandas 결측값 → None | 모델 저장 흐름 |
| 제품군 폼 변경 시 항목 수 안내가 늦게 바뀜 | 제품군 선택을 폼 밖으로 이동 | 스피커 선택 후 8개 표시·8개 복사 |
| 사용자 문자열이 엑셀 수식으로 해석됨 | 문자열 셀을 명시적 텍스트로 저장 | 실제 xlsx 재개방 후 값·셀 타입 확인 |
| 검증 명령·CI·에이전트 안내 부재 | `verify.py`, pytest, Ruff, Windows/Linux CI, `AGENTS.md` | 로컬 단일 명령 통과 |
| 현재 기능과 맞지 않는 README, 무조건 파일 복사 백업 안내 | 현재 기능·개발 절차, 앱 중지 후 백업으로 정정 | 문서 검토 |

## 측정 결과

임시 SQLite DB, 모델 100개/시험항목 1,700개, 동일 데이터, 조회 경로 15회 중앙값:

- 기존 통계 데이터 읽기: SELECT 101회, 73.13ms.
- 개선 후: SELECT 2회, 10.72ms (약 85% 감소).
- 두 경로의 반환 데이터가 동일한지 비교했다. 초기화·UI 렌더·네트워크를 제외한 로컬 조회 측정이며 전체 페이지 속도 보장은 아니다.

`python verify.py`: Ruff 통과, 회귀 테스트 18개 통과 (Windows / Python 3.12).
직접 의존성은 검증한 Streamlit 1.63.0 / pandas 3.0.5 / openpyxl 3.1.5로 고정했다.
전이 의존성 전체를 고정하는 lockfile은 도입하지 않았다.

## 검증 범위와 남은 한계

- AppTest는 로그인→생성→저장→메뉴 이동→로그아웃 및 통계/캐시를 실행한다. 브라우저 픽셀·캔버스 직접 조작은 이번에 검증하지 않았다.
- 운영 DB와 배포 서버에는 접근하지 않았다. 기존 데이터 마이그레이션은 임시 DB로 검증했다.
- 같은 표를 여러 사용자가 동시에 편집하면 마지막 저장이 적용되는 기존 정책을 유지한다. 충돌 감지나 항목별 diff 이력은 별도 설계가 필요하다.
- 비교표의 HTML은 다중 항목/테마 표현과 escape 검증이 있으므로 유지했다. 프레임워크 교체·페이지 분할·권한 시스템 추가는 하지 않았다.
- `context-notes.md`의 과거 수동 검증 기록은 변경하지 않았다. 현재 검증 기준은 `verify.py`와 테스트다.
